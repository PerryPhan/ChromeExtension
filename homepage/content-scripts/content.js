$(function(){
    // This work for controlling present's scripts 
    // if u want to reuse interface
    console.log('Homepage extension is loading...');
    VanillaTilt.init(document.querySelectorAll(".card"), {
        max: 25,
        speed: 400
    });
});
